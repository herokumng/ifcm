package com.dkbmc.ifcm.module.data.convert;

public class DataTypeDef {
	public enum systemType {
		DEFAULT, SALESFORCE, ORACLE
	}

	// salesforce field type
	public enum salesforceType {
		ADDRESS, ANYTYPE, CALCULATED, COMBOBOX, CURRENCY, DATACATEGORYGROUPREFERENCE, EMAIL, ENCRYPTEDSTRING, ID,
		JUNCTIONIDLIST, LOCATION, MASTERRECORD, MULTIPICKLIST, PERCENT, PHONE, PICKLIST, REFERENCE, TEXTAREA, URL,
		BASE64, BOOLEAN, BYTE, DATE, DATETIME, DOUBLE, INT, STRING, TIME
	}

	// oracle field Type
	public enum oracleType {
		CHAR, VARCHAR, VARCHAR2, NCHAR, NVARCHAR, LONG, CLOB, NCLOB, NUMBER, FLOAT, BINARY_FLOAT, BINARY_DOUBLE, DATE, TIMESTAMP,
		BLOB, BFILE, LONG_RAW
	}

	// java
	public enum moduleType {
		BOOLEAN, INT, LONG, FLOAT, DOUBLE, SHORT, BYTE, NUMBER, BIGINTEGER, BIGDECIMAL, STRING, CHAR, BYTE_ARRAY,  CHAR_ARRAY,  OBJECT, DATE, TIME, TIMESTAMP
	}
}
